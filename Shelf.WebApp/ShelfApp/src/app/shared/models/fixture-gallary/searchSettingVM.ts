export interface SearchSettingVM {
    id: string;
    itemVisibility: boolean;
    searchType: string;
    searchVisibility: boolean;
    selectedField: string;
    isAzSearch: boolean;
    selectedType: string;
}