export enum ParentAppType {
  Shelf = 'iShelf',
  Assort = 'iAssort',
  Allocate = 'allocate',
  WebView = 'vmode',
}

export enum AssortMode {
    Auto = 1,
    iAssortNICI = 2,
}

export enum AllocateMode {
    Manual = 1,
    Review = 2,
}

export enum AllocateProjectType {
    Reset = 1,
    NICI = 2,
}