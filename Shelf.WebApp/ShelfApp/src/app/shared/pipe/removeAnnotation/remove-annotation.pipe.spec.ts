import { RemoveAnnotationPipe } from './remove-annotation.pipe';

describe('RemoveAnnotationPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveAnnotationPipe();
    expect(pipe).toBeTruthy();
  });
});
