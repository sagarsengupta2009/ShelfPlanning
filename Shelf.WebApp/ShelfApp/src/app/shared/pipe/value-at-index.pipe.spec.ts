import { ValueAtIndexPipe } from './value-at-index.pipe';

describe('ValueAtIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new ValueAtIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
