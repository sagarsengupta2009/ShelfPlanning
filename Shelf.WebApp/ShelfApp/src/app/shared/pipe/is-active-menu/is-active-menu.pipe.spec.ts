import { IsActiveMenuPipe } from './is-active-menu.pipe';

describe('IsActiveMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new IsActiveMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
