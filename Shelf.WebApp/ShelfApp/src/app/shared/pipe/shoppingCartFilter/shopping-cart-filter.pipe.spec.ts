import { ShoppingCartFilterPipe } from './shopping-cart-filter.pipe';

describe('ShoppingCartFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShoppingCartFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
