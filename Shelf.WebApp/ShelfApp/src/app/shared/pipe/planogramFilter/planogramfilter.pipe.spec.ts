import { PlanogramfilterPipe } from './planogramfilter.pipe';

describe('PlanogramfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanogramfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
