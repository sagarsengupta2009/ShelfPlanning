import { AllowDecimalNumbersDirective } from './numbers-only.directive';

describe('AllowDecimalNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowDecimalNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
