import { MenuToolTipDirective } from './menu-tool-tip.directive';

describe('MenuToolTipDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuToolTipDirective();
    expect(directive).toBeTruthy();
  });
});
