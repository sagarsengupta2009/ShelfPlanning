import { ImgFallbackDirective } from './position-image-fallback.directive';

describe('ImgFallbackDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgFallbackDirective();
    expect(directive).toBeTruthy();
  });
});