import { RubberbandSupportDirective } from './rubberband-support.directive';

describe('RubberbandSupportDirective', () => {
  it('should create an instance', () => {
    const directive = new RubberbandSupportDirective();
    expect(directive).toBeTruthy();
  });
});
